query Issue($owner: String!, $repository: String!, $number: Int!) {
  repository(owner: $owner, name: $repository) {
    issue(number: $number) {
      title
      bodyHTML
      comments(last: 5) {
        edges {
          cursor
          node {
            author {
              login
            }
            bodyHTML
            createdAt
          }
        }
      }
    }
  }
}
